# ChromaDB Server Configuration
# ==================================================================
# VITE_CHROMADB_HOST - Full URL of the ChromaDB server including protocol
#
# The URL is parsed to extract:
#   - Protocol (http or https)
#   - Host (hostname and port)
#
# Examples:
#   VITE_CHROMADB_HOST=http://localhost:8000      (local development)
#   VITE_CHROMADB_HOST=https://chroma.example.com (production with TLS)
#
# Default: http://localhost:8000
# ==================================================================
VITE_CHROMADB_HOST=http://localhost:8000

# ==================================================================
# Additional settings available on the login screen:
#   - Tenant: ChromaDB tenant name (default: default_tenant)
#   - Database: ChromaDB database name (default: default_database)
#
# These values can be configured at runtime via the login screen UI.
# ==================================================================
