apiVersion: v1
kind: Service
metadata:
  name: {{ include "chromadb-ui.fullname" . }}
  labels:
    {{- include "chromadb-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chromadb-ui.selectorLabels" . | nindent 4 }}
